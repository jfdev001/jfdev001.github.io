# ICON time stepping scheme accuracy

**Session ID:** ses_015bd0936ffecKxeUvhUMbXhIL
**Created:** 8/10/2026, 8:01:24 AM
**Updated:** 8/10/2026, 8:09:37 AM

---

## User

is the following a roughly accurate characterization of the ICON time stepping scheme?? u^{n+1} = u^n + \Delta t \left[ \left(\frac{\delta
    u}{\delta t}\right)_{\text{dynamics}} + \boxed{\left(\frac{\delta u}{\delta t}\right)_{\text{GWD}} + \left(\frac{\delta u}{\delta t}\right)_{\text{turbulence}}} + ... \right] 

---

## Assistant (Plan · DeepSeek V4 Flash Free · 114.2s)

_Thinking:_

The user is asking about the ICON time stepping scheme, specifically whether their characterization is accurate. This is a question about the ICON (Icosahedral Nonhydrostatic) model's time integration scheme used in numerical weather prediction.

Let me analyze their equation:

u^{n+1} = u^n + Δt[(δu/δt)_dynamics + (δu/δt)_GWD + (δu/δt)_turbulence + ...]

This is asking about the numerically explicit time stepping with a split of the tendencies into dynamics and physics (GWD = gravity wave drag, turbulence).

Let me think about what I know about ICON's time stepping. First, let me check what's in the repository since we're in an "icon-model-public" directory. Maybe there's relevant source code.

Let me explore the repo to see if there's relevant code about time stepping.

Actually, let me think about the physics of this question first, then check the repo for confirmation.

ICON (ICOsahedral Non-hydrostatic) model uses a semi-implicit time integration scheme with split explicit or predictor-corrector approaches. The ICON time stepping is based on a semi-implicit RK (Runge-Kutta) scheme - specifically a third-order Runge-Kutta. Actually, ICON was historically developed from the old LM (Lokal-Modell), and uses the RK3 scheme from Wicker and Skamarock.

In ICON, the coupling between dynamics and physics follows. Let me recall details:

ICON's time integration:
- The dynamical core uses a predictor-corrector or RK scheme. ICON uses a semi-implicit time-stepping combining:
  - For the fast waves (acoustic/gravity), it uses a semi-implicit approach where the RHS includes linear terms handled implicitly.
  - Actually ICON uses a "semi-implicit predictor-corrector" scheme based on the work of Zängl? No...

Let me recall the actual ICON numerics. From the ICON documentation paper (Zängl et al. 2015, "The ICON (ICOsahedral Non-hydrostatic) modelling framework of DWD and MPI-M: A description of the non-hydrostatic dynamical core"):

The time integration in ICON's dynamical core is a 2-stage predictor... Actually, IIRC it's based on the semi-implicit scheme with a temporal discretization similar to the old... Hmm.

Let me recall. The ICON dynamical core time stepping:
- time discretization is a second-order accurate, semi-implicit, centered-in-time approach for the linear (fast) terms combined with a third-order... 

Actually I recall: ICON uses the explicit RK scheme with 3 stages? Or "the temporal discretization is the 'time-split' approach"?

I think the accurate description: ICON uses a semi-implicit, time-split scheme. The nonhydrostatic equations are integrated with a two-time-level scheme. Fast waves are treated semi-implicitly. There's a "predictor-corrector" for... Hmm.

From the ICON paper: "The temporal discretization used in the dynamical core is based on a Runge–Kutta implicit... " no.

Let me actually recall the key: From Zängl et al. 2015, Section 3 (Time integration):

The ICON time-Stepping is a "semi-implicit, predictor-corrector, two-time-level scheme". Hmm, actually I recall it being:

"the equations are time integrated with a second-order accurate iterative scheme" - no.

I recall the details: ICON uses an explicit third-order (for advection) and semi-implicit time treatment. Actually there's the "Runge-Kutta" in ICON: "a two-stage, third-order Runge-Kutta scheme" (RK3 from Wicker-Skamarock) for the advection and "the fast motions solved semi-implicitly."

But actually ICON dynamics time step: From the documentation, ICON uses the "split-explicit" approach? Hmm.

Let me get the details right. I believe:

The ICON dynamical core uses the following time discretization:
- In the horizontal: for fast-propagating waves, semi-implicit treatment of terms involving pressure gradient and divergence.
- The scheme is a "two-time-level" semi-implicit predictor-corrector (based on Gent? no).

Actually I now recall more precisely. From the ICON ModellingFramework paper, §3.1: "The temporal discretization combines a second-order accurate, semi-implicit method for the linear terms with an explicit scheme for the nonlinear terms" — something like that. And the vertical... Hmm.

Actually the well-known fact: ICON dynamical core time step is an RK-type: In the nonhydrostatic solver, terms are split:
1. The fast terms (acoustic waves, gravity waves) that require small time steps are... no, ICON does not sub-step the fast modes — it treats them semi-implicitly, avoiding the need for short acoustic time steps.

Yes: ICON treats the horizontal propagation of acoustic/gravity waves semi-implicitly. So no time splitting into multiple sub-steps for the fast waves. Vertical propagation uses implicit treatment.

Regarding the coupling with physics: In ICON, the physics tendencies are added explicitly. The classic approach: u^{n+1} = u^n + Δt * RHS where RHS = dynamics tendencies + physics tendencies (rad, GWD, turbulence, convection, etc.).

This matches the user's equation form.

Now, is the characterization roughly accurate? The user writes:

u^{n+1} = u^n + Δt[(δu/δt)_dynamics + (δu/δt)_GWD + (δu/δt)_turbulence + ...]

Potential inaccuracies/nuances:

1. ICON's time stepping is not a simple explicit forward-Euler update. ICON uses a semi-implicit scheme: dynamical tendencies themselves contain implicit (Crank-Nicolson-like) treatment of certain linear terms. So writing it as a single explicit Euler step is an oversimplification.

2. The dynamics tendency includes the advection, Coriolis, pressure gradient, and the physics tendencies are computed via parameterizations and added as tendencies. In ICON, the coupling of physics to dynamics: "the tendencies from the physics schemes are added to the time stepping" — this matches.

3. Also, GWD in ICON... does ICON have gravity wave drag? ICON (DWD) physics for NWP includes subgrid-scale orographic drag (gravity wave drag) and turbulent mixing. Yes. But for the ICON model, gravity wave drag (SSO - subgrid-scale orography scheme) is part of the physical parameterizations. And turbulence (turbulent diffusion / mixing, TKE). So those are physical tendencies.

4. Some terms are computed on different time steps or with tendencies computed at midpoints, etc.

Actually, I think this is a request essentially to validate the equation. The user is basically asking: "is it roughly right that the ICON time step update equals u plus dt times dynamics + physics tendencies?" Yes, that's a roughly accurate characterization at a high level, but with caveats: the time-stepping isn't a simple forward Euler — the dynamics uses semi-implicit/implicit methods and there are intermediate steps; the physics tendencies are indeed added this way (tendency coupling).

Since we're in plan mode and this is essentially a knowledge question (not a coding task), and the repo is icon-model-public, maybe I should check the repo for the actual time stepping code to give a grounded answer.

Let me look at the repo. But wait — plan mode says read-only; I can still read/search. This is fine.

Let me search the codebase for time stepping / nudging code.

Actually, this question seems like a conceptual question that might not require code. But I should ground it. Let me check the repo structure first.

Actually — the question is direct: "is the following a roughly accurate characterization of the ICON time stepping scheme?" The answer: Yes, roughly, with important caveats. Let me verify with the actual code in the repo if available to be precise.

Let me explore the repo.

Note the user uses δu/δt with boxed terms "GWD" and "turbulence". In ICON terminology, the physics is coupled: in the NWP model, "GWD" is gravity wave drag; "turbulence" could be TKE-based turbulent diffusion.

Also note: in ICON there's also the "fast" (dynamics) and "slow" (physics) split. Many models use this operator-split/additive form conceptually.

Let me look at the repo to confirm how ICON does it. Let me search.

Given plan mode, I'll just search and answer. This is an informational question, so no plan to build files. But the system reminder says my responsibility is to construct a plan... but this is really a question, not a task. I should just answer the question. Plan mode restricts edits, but answering a question is fine.

Let me do a quick search in the repo to ground the answer.

Actually, since this is purely a conceptual physics question, and the user is asking for validation, I could answer directly. But grounding in the actual ICON code would make the answer more accurate. Let me quickly search.

Yes — that's a reasonable high-level picture, but with two important caveats. Let me check the actual ICON code to be

**Tool: bash**

**Input:**
```json
{
  "command": "ls"
}
```

**Output:**
```
AUTHORS.TXT
collect.extra-libs.in
config
config.h.in
configure
configure.ac
CONTRIBUTING.md
cpd_report
data
depgen.c.config.in
depgen.f90.config.in
deplist.config.in
doc
etc
externals
icon.mk.in
inlib.mk.in
LICENSES
m4
Makefile.in
make_runscripts
planning
ragnarok
README.md
RELEASE_NOTES.md
REUSE.toml
run
schedulers
scripts
src
support
test
utils
vertical_coord_tables

```

**Tool: grep**

**Input:**
```json
{
  "pattern": "GWD|gwd",
  "include": "*.f90"
}
```

**Output:**
```
Found 100 matches (more matches available)
/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_gw_interface.f90:
  Line 15: ! inwp_gwd == 1 == IFS non-orographic gravity wave drag

  Line 38:   USE mo_gwd_wms,              ONLY: gwdrag_wms

  Line 53:   PUBLIC  ::  nwp_gwdrag

  Line 59:   SUBROUTINE nwp_gwdrag  (   tcall_sso_jg,              & !>input

  Line 61:                          &   tcall_gwd_jg,              & !>input

  Line 62:                          &   lcall_gwd_jg,              & !>input

  Line 80:     REAL(wp),  INTENT(in)   :: tcall_gwd_jg    !< time interval for gwd

  Line 81:     LOGICAL ,  INTENT(in)   :: lcall_gwd_jg    !< .TRUE.: gwd scheme is actually called

  Line 218:       IF (lcall_gwd_jg .AND. atm_phy_nwp_config(jg)%inwp_gwd == 1  &

  Line 221:     ! but inwp_gwd=1 is still needed for shared infrastructure

  Line 226:         ! get total precipitation rate [kg/m2/s] ==> input for gwdrag_wms

  Line 235:           !IF (lozpr .AND. ((ngauss==2) .OR. (ngauss==4)) & ! these two variables would have to be imported from data_gwd

  Line 241:         CALL gwdrag_wms(                                   &

  Line 247:            & ptstep   = tcall_gwd_jg                    ,  & !< in:  time step

  Line 256:            & ptenu    = prm_nwp_tend%ddt_u_gwd  (:,:,jb),  & !< out: u-tendency

  Line 257:            & ptenv    = prm_nwp_tend%ddt_v_gwd  (:,:,jb),  & !< out: v-tendency

  Line 258:            & pfluxu   = z_fluxu (:,:)                   ,  & !< out: zonal  GWD vertical mom flux

  Line 259:            & pfluxv   = z_fluxv (:,:)                   ,  & !< out: merid. GWD vertical mom flux

  Line 268:           prm_nwp_tend%ddt_u_gwd(jc,1,jb) = 0.125_vp*prm_nwp_tend%ddt_u_gwd(jc,1,jb)

  Line 269:           prm_nwp_tend%ddt_v_gwd(jc,1,jb) = 0.125_vp*prm_nwp_tend%ddt_v_gwd(jc,1,jb)

  Line 272:         ! Limit also gwdrag wind tendencies. They can become numerically unstable in the upper mesosphere

  Line 278:             prm_nwp_tend%ddt_u_gwd(jc,jk,jb) = &

  Line 279:               SIGN(MIN(0.05_vp,ABS(prm_nwp_tend%ddt_u_gwd(jc,jk,jb))),prm_nwp_tend%ddt_u_gwd(jc,jk,jb))

  Line 280:             prm_nwp_tend%ddt_v_gwd(jc,jk,jb) = &

  Line 281:               SIGN(MIN(0.05_vp,ABS(prm_nwp_tend%ddt_v_gwd(jc,jk,jb))),prm_nwp_tend%ddt_v_gwd(jc,jk,jb))

  Line 288:       ! SPPT perturbation for SSO and non-orographic GWD

  Line 289:       IF ( ANY(itype_stoch_phys == (/2,3,4/)) .AND. (lcall_sso_jg .OR. lcall_gwd_jg) ) THEN

  Line 301:         IF (lcall_gwd_jg .AND. atm_phy_nwp_config(jg)%inwp_gwd == 1) THEN

  Line 306:               prm_nwp_tend%ddt_u_gwd(jc,jk,jb) = (1.0_vp+pertb)*prm_nwp_tend%ddt_u_gwd(jc,jk,jb)

  Line 307:               prm_nwp_tend%ddt_v_gwd(jc,jk,jb) = (1.0_vp+pertb)*prm_nwp_tend%ddt_v_gwd(jc,jk,jb)

  Line 319:   END SUBROUTINE nwp_gwdrag


/home/jf01/dev/iap/icon-model-public/src/namelists/mo_nwp_phy_nml.f90:
  Line 45:     &                               config_icpl_gwd_prec   => icpl_gwd_prec

  Line 61:   REAL(wp) :: dt_gwd(max_dom)    !! "-"  for subscale gravity waves

  Line 87:   INTEGER  :: inwp_gwd(max_dom)           !! non-orographic gravity wave drag

  Line 112:   INTEGER  :: icpl_gwd_prec      !! introduce linear dependence of gwd momentum flux to precipitation

  Line 144:     &                    inwp_radiation, inwp_sso, inwp_gwd,         &

  Line 147:     &                    dt_conv, dt_rad, dt_sso, dt_gwd, dt_ccov,   &

  Line 171:     &                    icpl_gwd_prec

  Line 197:     REAL(wp) :: dt_conv_def, dt_rad_def, dt_sso_def, dt_gwd_def

  Line 209:     dt_gwd_def  = 1200._wp

  Line 221:     inwp_gwd(:)        = param_def

  Line 230:     dt_gwd  (:) = dt_gwd_def

  Line 291:     icpl_gwd_prec      = 0        ! If proportionality between gwd-momentum flux and total precipitation exists

  Line 293:                                   ! 1 - increase in gwd mom. flux; being proportional to total precipitation

  Line 335:                             ! 1 = SSO + GWD + Rayleigh friction

  Line 343:     itype_stoch_phys = 0    ! 0 = none, 1 = CONV only, 2 = GWD only

  Line 344:                             ! 3 = CONV + GWD using single stochastic pattern (SPPT-like)

  Line 345:                             ! 4 = CONV + GWD using uncorrelated patterns (iSPPT-like)

  Line 392:       inwp_gwd(:)        = -1

  Line 401:       dt_gwd  (:) = -999._wp

  Line 417:       IF (inwp_gwd(1)        < 0) inwp_gwd(1)        = param_def  !> 1 = Orr-Ern-Bechthold scheme (IFS)

  Line 426:       IF (dt_gwd  (1) < 0._wp) dt_gwd  (1) = dt_gwd_def

  Line 444:         IF (inwp_gwd(jg)        < 0) inwp_gwd(jg)        = inwp_gwd(jg-1)

  Line 453:         IF (dt_gwd  (jg) < 0._wp) dt_gwd  (jg) = dt_gwd  (jg-1)

  Line 625:       atm_phy_nwp_config(jg)%inwp_gwd        = inwp_gwd(jg)

  Line 651:       atm_phy_nwp_config(jg)%dt_gwd          = dt_gwd  (jg)

  Line 691:     config_icpl_gwd_prec         = icpl_gwd_prec


/home/jf01/dev/iap/icon-model-public/src/namelists/mo_ser_nml.f90:
  Line 50:   INTEGER, TARGET ::  ser_gwdrag(3)

  Line 66:   &                  ser_turbtrans, ser_turbdiff, ser_gwdrag, ser_time_loop_end, ser_reset_to_initial_state, &

  Line 106:    ser_gwdrag = param_def


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_gpu_util.f90:
  Line 139:       !$ACC   HOST(a%ldetrain_conv_prec, a%inwp_radiation, a%inwp_sso, a%inwp_gwd, a%inwp_cldcover, a%inwp_turb) &

  Line 140:       !$ACC   HOST(a%inwp_surface, a%itype_z0, a%dt_conv, a%dt_ccov, a%dt_rad, a%dt_sso, a%dt_gwd, a%dt_fastphy) &

  Line 254:       !$ACC   DEVICE(a%ldetrain_conv_prec, a%inwp_radiation, a%inwp_sso, a%inwp_gwd, a%inwp_cldcover, a%inwp_turb) &

  Line 255:       !$ACC   DEVICE(a%inwp_surface, a%itype_z0, a%dt_conv, a%dt_ccov, a%dt_rad, a%dt_sso, a%dt_gwd, a%dt_fastphy) &


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_phy_types.f90:
  Line 623:       ddt_u_gwd       (:,:,:)  ,& !! ZonalW-tendency from gravity wave drag

  Line 625:       ddt_v_gwd       (:,:,:)  ,& !! MeridW-tendency from gravity wave drag


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_turbdiff_interface.f90:
  Line 557:       ! Add GWD momentum tendency to SSO when dissipative heating is calculated

  Line 558:       IF (tdc%ltmpcor .AND. atm_phy_nwp_config(jg)%inwp_gwd > 0) THEN

  Line 563:             ut_sso(jc,jk) = ut_sso(jc,jk) + prm_nwp_tend%ddt_u_gwd(jc,jk,jb)

  Line 564:             vt_sso(jc,jk) = vt_sso(jc,jk) + prm_nwp_tend%ddt_v_gwd(jc,jk,jb)


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_msgwam_utils.f90:
  Line 74:   USE data_gwd,    ONLY : nslope, gfluxlaun, &

  Line 1501:     REAL(wp)              :: ddt_theta_gwd_mgm

  Line 1817: !$OMP            inv_rho, inv_dzrho, fc_o_rho_th, ddt_theta_gwd_mgm)

  Line 1842:           p_fld%ddt_u_gwd_mgm(jc,jk,jb) = + p_fld%mfcxz_mgm(jc,jk,jb) & ! vert flux conv.

  Line 1845:           p_fld%ddt_u_gwd_mgm(jc,jk,jb) = + p_fld%mfcxz_mgm(jc,jk,jb) & ! vert flux conv.

  Line 1855:           p_fld%ddt_v_gwd_mgm(jc,jk,jb) = p_fld%mfcyz_mgm(jc,jk,jb) & ! vert flux conv.

  Line 1858:           p_fld%ddt_v_gwd_mgm(jc,jk,jb) = p_fld%mfcyz_mgm(jc,jk,jb) & ! vert flux conv.

  Line 1868:           p_fld%ddt_u_gwd_pmom_mgm(jc,jk,jb) = p_fld%pmfcxz_mgm(jc,jk,jb)   ! vert flux conv.

  Line 1870:           p_fld%ddt_u_gwd_pmom_mgm(jc,jk,jb) = p_fld%pmfcxz_mgm(jc,jk,jb) & ! vert flux conv.

  Line 1879:           p_fld%ddt_v_gwd_pmom_mgm(jc,jk,jb) = p_fld%pmfcyz_mgm(jc,jk,jb)   ! vert flux conv.

  Line 1881:           p_fld%ddt_v_gwd_pmom_mgm(jc,jk,jb) = p_fld%pmfcyz_mgm(jc,jk,jb) & ! vert flux conv.

  Line 1887:           ddt_theta_gwd_mgm = - ptfldiv_hor(jc,jk,jb)*inv_rho   ! hori flux conv.

  Line 1889:           p_fld%ddt_pt_gwd_mgm(jc,jk,jb) = ddt_theta_gwd_mgm

  Line 1892:           p_fld%ddt_t_gwd_mgm(jc,jk,jb) = ddt_theta_gwd_mgm &

  Line 1895:           p_fld%ddt_t_gwd_mgm(jc,jk,jb) = 0._wp

  Line 1906:           &              p_fld%ddt_u_gwd_mgm(:,1:nlevsmootht+1,jb))

  Line 1908:           &              p_fld%ddt_v_gwd_mgm(:,1:nlevsmootht+1,jb))

  Line 1910:           &              p_fld%ddt_u_gwd_pmom_mgm(:,1:nlevsmootht+1,jb))

  Line 1912:           &              p_fld%ddt_v_gwd_pmom_mgm(:,1:nlevsmootht+1,jb))

  Line 1914:           &              p_fld%ddt_t_gwd_mgm(:,1:nlevsmootht+1,jb))

  Line 1935:       &  p_patch%cells%center(:,:)%lat, p_patch%cells%center(:,:)%lon, p_fld%ddt_u_gwd_mgm, 'ddt_u_gwd_mgm', 1)

  Line 1937:       &  p_patch%cells%center(:,:)%lat, p_patch%cells%center(:,:)%lon, p_fld%ddt_v_gwd_mgm, 'ddt_v_gwd_mgm', 1)

  Line 1939:       &  p_patch%cells%center(:,:)%lat, p_patch%cells%center(:,:)%lon, p_fld%ddt_t_gwd_mgm, 'ddt_t_gwd_mgm', 1)

  Line 1941:       &  p_patch%cells%center(:,:)%lat, p_patch%cells%center(:,:)%lon, p_fld%ddt_u_gwd_pmom_mgm, 'ddt_u_gwd_pmom_mgm', 1)

  Line 1943:       &  p_patch%cells%center(:,:)%lat, p_patch%cells%center(:,:)%lon, p_fld%ddt_v_gwd_pmom_mgm, 'ddt_v_gwd_pmom_mgm', 1)


/home/jf01/dev/iap/icon-model-public/externals/msgwam/msgwam/mo_gw_source_bg.f90:
  Line 36:   USE data_gwd,    ONLY : nslope, gfluxlaun, &

  Line 156:     !                    based on mo_gwd_wms.f90

  Line 191:     ! PART 1: calculate launch spectrum in (c,phi) space (based on mo_gwd_wms.f90)

  Line 811:               ! with Scinocca (2003), i.e. mo_gwd_wms.f90.


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90:
  Line 41:     &                                   itsso, itgwd, itfastphy, icosmo, igme, ivdiff,               &


(Results truncated. Consider using a more specific path or pattern.)
```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 26.9s)

_Thinking:_

The user is asking a conceptual question about the ICON time stepping scheme. This is not really a coding task - it's asking me to evaluate whether their characterization is accurate. This seems to be a knowledge/conceptual question, not a repo modification task.

Let me think about what I know about ICON's time stepping scheme.

The ICON model time integration: The user proposes:
u^{n+1} = u^n + Δt[ (du/dt)_dynamics + (du/dt)_GWD + (du/dt)_turbulence + ... ]

This is a "process splitting" / tend-physics coupling characterization. Let me think about what the actual ICON scheme does.

In ICON, the time stepping is done in `mo_nh_solve` (nonhydrostatic core) and the physics coupling is done in `nwp_nh_interface` or `mo_nh_interface_nwp`. The atmospheric evolution equation is typically written:

∂ψ/∂t = ...dynamics terms + physics tendencies

The physical parameterizations produce tendencies (e.g., ddt_u_gwd, ddt_v_turb) that are added to the dynamics.

Actually the standard ICON formulation: The dynamical core integrates prognostic equations of the form:

∂ψ/∂t = f_dyn(ψ) + P_phys

where P_phys are the physics tendencies (radiation, turbulence, GWD, convection, etc.).

The time stepping in the dynamics uses a semi-implicit scheme (predictor-corrector, with an implicit treatment of fast waves). Actually, ICON uses a two-time-level scheme with a predictor-corrector... Let me recall.

Actually the ICON dynamical core uses the RK-type. Hmm. Let me recall the details:

From the ICON documentation (Zängl et al. 2015, "The ICON (ICOsahedral Non-hydrostatic) modelling framework"), the time integration is:

The ICON nonhydrostatic dynamical core uses a predictor-corrector type time integration that is a variant of the scheme described by ... Actually, ICON was developed from the "LM" (Lokal-Modell). The ICON dynamical core time integration... let me recall.

From the ICON documentation paper: "The temporal discretization of the ICON dynamical core is based on a two-time-level scheme with a predictor-corrector approach". The fast waves are handled semi-implicitly.

Actually let me recall more precisely. The ICON dynamical core: time integration is a variant of the RK2 scheme... Hmm.

Let me look at the actual source. Since we have the repo, I could look at the actual code. Let me search for the relevant routines.

Actually, this is a QA-type question. The user wants to know whether their characterization of the ICON time stepping is roughly accurate. Given plan mode, I should research and then present the answer.

Let me check the actual code in the repo to see how tendencies are combined. Let me look at mo_nh_solve.f90 and the physics coupling.

Let me explore.

Let me look at the actual ICON source to ground the answer

**Tool: bash**

**Input:**
```json
{
  "command": "ls src/atm_dyn_iconamo/ 2>/dev/null; ls src/ | head -50"
}
```

**Output:**
```
advection
atm_dyn_iconam
atm_phy_aes
atm_phy_bcs
atm_phy_les
atm_phy_nwp
atm_phy_rte_rrtmgp
atm_phy_schemes
comin
configure_model
coupling
data_assimilation
diagnostics
drivers
granules
grid
hamocc
include
io
lnd_phy_nwp
lnd_phy_schemes
namelists
ocean
parallel_infrastructure
sea_ice
serialization
shared
shr_horizontal
sppt
templates
testbed
testcases
upatmo
waves

```

**Tool: grep**

**Input:**
```json
{
  "pattern": "SUBROUTINE solve_nh|SUBROUTINE*.*nudge|tend_physics|ddt_uel|pdt_*topo_fac",
  "include": "*solve*.f90"
}
```

**Output:**
```
Found 2 matches
/home/jf01/dev/iap/icon-model-public/src/atm_dyn_iconam/mo_solve_nonhydro.f90:
  Line 102:   SUBROUTINE solve_nh (p_nh, p_patch, p_int, prep_adv, nnow, nnew, l_init, l_recompute, lsave_mflx, &

  Line 2916:   END SUBROUTINE solve_nh

```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 22.0s)

_Thinking:_

Let me look at the dynamics core solve_nh and how it couples with physics. The user question is about the ICON time stepping scheme. Let me examine the key routines. Let me look at the main dynamics file and how physics tendencies are incorporated.

Let me look at the key dynamics routines and how physics tendencies get incorporated

**Tool: grep**

**Input:**
```json
{
  "include": "*.f90",
  "pattern": "p_int%ddt_\\(u|v\\|w\\|exner\\|temp\\|rho\\)|ddt_u_adv|nudge_absent"
}
```

**Output:**
```
Found 10 matches
/home/jf01/dev/iap/icon-model-public/src/atm_phy_les/mo_ls_forcing.f90:
  Line 1110:                               ddt_u_adv_ls, ddt_v_adv_ls, wsub,        & !output

  Line 1130:     REAL(wp),       INTENT(out)             :: ddt_u_adv_ls(:)

  Line 1178:       ddt_u_adv_ls    = ddt_u_hadv_ls   (:,n_curr)*(1.-int_weight) + ddt_u_hadv_ls   (:,n_next)*int_weight

  Line 1190:       ddt_u_adv_ls    = ddt_u_hadv_ls(:,1)

  Line 1270:         ddt_u_ls = ddt_u_ls + ddt_u_adv_ls


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_phy_types.f90:
  Line 640:       ddt_u_adv_ls    (:),    &   !! LS tendency for u-wind from advection


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90:
  Line 2308:         &                     prm_nwp_tend%ddt_u_adv_ls,        & !>out


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_phy_state.f90:
  Line 7309:       ALLOCATE(phy_tend%ddt_u_adv_ls(klev),STAT=ist)

  Line 7312:                     'allocation for phy_tend%ddt_u_adv_ls failed')

  Line 7314:       phy_tend%ddt_u_adv_ls = 0._wp

```

**Tool: grep**

**Input:**
```json
{
  "include": "*.f90",
  "pattern": "SUBROUTINE nwp_nh_interface|SUBROUTINE nep_interface|call_turb|call_gwd|call_sso"
}
```

**Output:**
```
Found 52 matches
/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_gw_interface.f90:
  Line 59:   SUBROUTINE nwp_gwdrag  (   tcall_sso_jg,              & !>input

  Line 60:                          &   lcall_sso_jg,              & !>input

  Line 61:                          &   tcall_gwd_jg,              & !>input

  Line 62:                          &   lcall_gwd_jg,              & !>input

  Line 78:     REAL(wp),  INTENT(in)   :: tcall_sso_jg    !< time interval for sso

  Line 79:     LOGICAL ,  INTENT(in)   :: lcall_sso_jg    !< .TRUE.: sso scheme is actually called

  Line 80:     REAL(wp),  INTENT(in)   :: tcall_gwd_jg    !< time interval for gwd

  Line 81:     LOGICAL ,  INTENT(in)   :: lcall_gwd_jg    !< .TRUE.: gwd scheme is actually called

  Line 148:       IF (lcall_sso_jg .AND. atm_phy_nwp_config(jg)%inwp_sso == 1) THEN

  Line 178:           & pdt       =tcall_sso_jg                     ,  & !< in:  time step

  Line 218:       IF (lcall_gwd_jg .AND. atm_phy_nwp_config(jg)%inwp_gwd == 1  &

  Line 247:            & ptstep   = tcall_gwd_jg                    ,  & !< in:  time step

  Line 289:       IF ( ANY(itype_stoch_phys == (/2,3,4/)) .AND. (lcall_sso_jg .OR. lcall_gwd_jg) ) THEN

  Line 291:         IF (lcall_sso_jg .AND. atm_phy_nwp_config(jg)%inwp_sso > 0) THEN

  Line 301:         IF (lcall_gwd_jg .AND. atm_phy_nwp_config(jg)%inwp_gwd == 1) THEN


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_turbtrans_interface.f90:
  Line 89: SUBROUTINE nwp_turbtrans  ( tcall_turb_jg,                     & !>in

  Line 114:   REAL(wp),                    INTENT(in)   :: tcall_turb_jg   !< time interval for

  Line 571:           &  dt_tke=tcall_turb_jg,                                                     & !in

  Line 901:             &  dt_tke=tcall_turb_jg,                                        & !in


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nwp_turbdiff_interface.f90:
  Line 85:   SUBROUTINE nwp_turbdiff  ( tcall_turb_jg,                     & !>in

  Line 112:   REAL(wp),                    INTENT(in)   :: tcall_turb_jg   !< time interval for turbulence

  Line 455:            &                          ptr=ptr(:), dt=tcall_turb_jg,                    &

  Line 588:         &  dt_var=tcall_turb_jg,                                                      & !in

  Line 589:         &  dt_tke=tcall_turb_jg,                                                      & !in

  Line 684:         &  dt_var=tcall_turb_jg,                                  & !in

  Line 770: !!$        p_diag%extra_2d(jc,jb,1) = p_diag%extra_2d(jc,jb,1) + tcall_turb_jg*prm_diag%qcfl_s(jc,jb)

  Line 771: !!$        p_diag%extra_2d(jc,jb,2) = p_diag%extra_2d(jc,jb,2) + tcall_turb_jg*prm_diag%qifl_s(jc,jb)

  Line 789:            &                          ptr=ptr(:), dt=tcall_turb_jg,                     &

  Line 856:         &                i_startidx=i_startidx, i_endidx=i_endidx, dt=tcall_turb_jg,   & !in

  Line 913:              &           + tcall_turb_jg*prm_nwp_tend%ddt_tracer_turb(jc,jk,jb,iqv))

  Line 915:          &  + cpd_o_cvd * tcall_turb_jg*prm_nwp_tend%ddt_temp_turb(jc,jk,jb)

  Line 916:         p_diag%u(jc,jk,jb) = p_diag%u(jc,jk,jb) + tcall_turb_jg*prm_nwp_tend%ddt_u_turb(jc,jk,jb)

  Line 917:         p_diag%v(jc,jk,jb) = p_diag%v(jc,jk,jb) + tcall_turb_jg*prm_nwp_tend%ddt_v_turb(jc,jk,jb)

  Line 929:              &           + tcall_turb_jg*prm_nwp_tend%ddt_tracer_turb(jc,jk,jb,iqc))

  Line 942:             &                              + tcall_turb_jg                                &

  Line 956:             &                             + tcall_turb_jg                               &

  Line 969:               &                              + tcall_turb_jg                                &

  Line 985:             &                             + tcall_turb_jg                               &

  Line 998:               &                              + tcall_turb_jg                                &

  Line 1012:               &                             + tcall_turb_jg                               &

  Line 1025:               &                             + tcall_turb_jg                               &


/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90:
  Line 199:   SUBROUTINE nwp_nh_interface(lcall_phy_jg, linit, lredgrid,       & !input

  Line 3053:   END SUBROUTINE nwp_nh_interface


/home/jf01/dev/iap/icon-model-public/src/atm_phy_les/mo_les_turb_interface.f90:
  Line 52: SUBROUTINE les_turbulence  ( tcall_turb_jg,                   & !>in

  Line 80:   REAL(wp),                    INTENT(in)   :: tcall_turb_jg   !< time interval for turbulence

  Line 127:                                      tcall_turb_jg,   & !in

  Line 146:                                    tcall_turb_jg      & !in

  Line 176:              &           + tcall_turb_jg*prm_nwp_tend%ddt_tracer_turb(jc,jk,jb,iqv))

  Line 178:          &  + tcall_turb_jg*prm_nwp_tend%ddt_temp_turb(jc,jk,jb)

  Line 179:         p_diag%u(jc,jk,jb) = p_diag%u(jc,jk,jb) + tcall_turb_jg*prm_nwp_tend%ddt_u_turb(jc,jk,jb)

  Line 180:         p_diag%v(jc,jk,jb) = p_diag%v(jc,jk,jb) + tcall_turb_jg*prm_nwp_tend%ddt_v_turb(jc,jk,jb)

  Line 190:              &           + tcall_turb_jg*prm_nwp_tend%ddt_tracer_turb(jc,jk,jb,iqc))

```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 14.1s)

_Thinking:_

Let me look at the key dynamics routine - mo_solve_nonhydro.f90 - to understand the actual time stepping scheme. Let me also look at how physics tendencies are added via ddt_ processes. Let me look at the general interface with process_ddt or similar.

Let me search for how physics tendencies get into the momentum equation in the dynamics.

Let me examine the core time stepping in the dynamics solver and how physics tendencies enter



**Tool: grep**

**Input:**
```json
{
  "include": "mo_solve_nonhydro.f90",
  "pattern": "ddt_|tend_|RK|Runge|predictor|startidx|Implicit|semi"
}
```

**Output:**
```
Found 100 matches (more matches available)
/home/jf01/dev/iap/icon-model-public/src/atm_dyn_iconam/mo_solve_nonhydro.f90:
  Line 85:   ! from terms xyz, ddt_vn_xyz, is disabled by default due to the fear that the

  Line 133:     INTEGER  :: i_startblk, i_endblk, i_startidx, i_endidx, ishift

  Line 219:     REAL(wp) :: z_ddt_vn_dyn, z_ddt_vn_apc, z_ddt_vn_cor, &

  Line 220:       &         z_ddt_vn_pgr, z_ddt_vn_ray, z_d_vn_dmp, z_d_vn_iau

  Line 421:       IF (istep == 1) THEN ! predictor step

  Line 498: !$OMP DO PRIVATE(jb,i_startidx,i_endidx,jk,jc,z_exner_ic,z_theta_v_pr_ic,z_w_backtraj,&

  Line 507:           i_startidx, i_endidx, rl_start, rl_end)

  Line 509:         IF (istep == 1) THEN ! to be executed in predictor step only

  Line 515:             DO jc = i_startidx, i_endidx

  Line 541:             DO jc = i_startidx, i_endidx

  Line 555:               DO jc = i_startidx, i_endidx

  Line 568:               DO jc = i_startidx, i_endidx

  Line 587:               DO jc = i_startidx, i_endidx

  Line 610:           DO jc = i_startidx, i_endidx

  Line 618:           DO jc = i_startidx, i_endidx

  Line 631:             DO jc = i_startidx, i_endidx

  Line 637:               ! (needed in the predictor step only)

  Line 654:             DO jc = i_startidx, i_endidx

  Line 686:             DO jc = i_startidx, i_endidx

  Line 701:             DO jc = i_startidx, i_endidx

  Line 772:           DO jc = i_startidx, i_endidx

  Line 795:               DO jc = i_startidx, i_endidx

  Line 826: !$OMP DO PRIVATE(jb,i_startidx,i_endidx,jk,jc) ICON_OMP_DEFAULT_SCHEDULE

  Line 829:           CALL get_indices_c(p_patch, jb, i_startblk, i_endblk, i_startidx, i_endidx, rl_start, rl_end)

  Line 835:             DO jc = i_startidx, i_endidx

  Line 925: !$OMP DO PRIVATE(jb,jk,je,i_startidx,i_endidx,ilc0,ibc0,lvn_pos,&

  Line 930:                            i_startidx, i_endidx, rl_start, rl_end)

  Line 943:             DO je = i_startidx, i_endidx

  Line 948:               DO je = i_startidx, i_endidx

  Line 1016:             DO je = i_startidx, i_endidx

  Line 1021:               DO je = i_startidx, i_endidx

  Line 1065: !$OMP DO PRIVATE(jb,jk,je,i_startidx,i_endidx) ICON_OMP_DEFAULT_SCHEDULE

  Line 1069:                              i_startidx, i_endidx, rl_start, rl_end)

  Line 1073:           DO je = i_startidx, i_endidx

  Line 1081:             DO je = i_startidx, i_endidx

  Line 1104: !$OMP DO PRIVATE(jb,i_startidx,i_endidx,jk,je,z_theta1,z_theta2,ikp1,ikp2) ICON_OMP_DEFAULT_SCHEDULE

  Line 1108:                              i_startidx, i_endidx, rl_start, rl_end)

  Line 1115:             DO je = i_startidx, i_endidx

  Line 1124:           DO je = i_startidx, i_endidx

  Line 1128:             DO je = i_startidx, i_endidx

  Line 1144:             DO je = i_startidx, i_endidx

  Line 1150:               DO je = i_startidx, i_endidx

  Line 1172:             DO je = i_startidx, i_endidx

  Line 1178:               DO je = i_startidx, i_endidx

  Line 1213:             DO je = i_startidx, i_endidx

  Line 1217:               DO je = i_startidx, i_endidx

  Line 1249:             DO je = i_startidx, i_endidx

  Line 1275:             DO je = i_startidx, i_endidx

  Line 1342: !$OMP DO PRIVATE(jb,i_startidx,i_endidx,jk,je,z_graddiv2_vn,                                                   &

  Line 1343: !$OMP            z_ddt_vn_dyn, z_ddt_vn_apc, z_ddt_vn_cor, z_ddt_vn_pgr, z_ddt_vn_ray, z_d_vn_dmp, z_d_vn_iau  &

  Line 1349:           i_startidx, i_endidx, rl_start, rl_end)

  Line 1354:           !$ACC LOOP GANG(STATIC: 1) VECTOR PRIVATE(z_ddt_vn_dyn, z_ddt_vn_apc, z_ddt_vn_cor, z_ddt_vn_pgr) TILE(32, 4)

  Line 1357:             DO je = i_startidx, i_endidx

  Line 1359:               z_ddt_vn_apc                      =  p_nh%diag%ddt_vn_apc_pc(je,jk,jb,nnow)*wgt_nnow_vel  &

  Line 1360:                 &                                 +p_nh%diag%ddt_vn_apc_pc(je,jk,jb,nnew)*wgt_nnew_vel

  Line 1361:               z_ddt_vn_pgr                      = -cpd*z_theta_v_e(je,jk,jb)*z_gradh_exner(je,jk,jb)

  Line 1363:               z_ddt_vn_dyn                      =  z_ddt_vn_apc                   & ! advection plus Coriolis

  Line 1364:                 &                                 +z_ddt_vn_pgr                   & ! pressure gradient

  Line 1365:                 &                                 +p_nh%diag%ddt_vn_phy(je,jk,jb)   ! physics applied in dynamics

  Line 1367:               p_nh%prog(nnew)%vn(je,jk,jb)      =  p_nh%prog(nnow)%vn(je,jk,jb)   + dtime       * z_ddt_vn_dyn

  Line 1370:               IF (p_nh%diag%ddt_vn_adv_is_associated .OR. p_nh%diag%ddt_vn_cor_is_associated) THEN

  Line 1371:                 z_ddt_vn_cor                    =  p_nh%diag%ddt_vn_cor_pc(je,jk,jb,nnow)*wgt_nnow_vel  &

  Line 1372:                   &                               +p_nh%diag%ddt_vn_cor_pc(je,jk,jb,nnew)*wgt_nnew_vel

  Line 1374:                 IF (p_nh%diag%ddt_vn_adv_is_associated) THEN

  Line 1375:                   p_nh%diag%ddt_vn_adv(je,jk,jb)=  p_nh%diag%ddt_vn_adv(je,jk,jb) + r_nsubsteps *(z_ddt_vn_apc-z_ddt_vn_cor)

  Line 1378:                 IF (p_nh%diag%ddt_vn_cor_is_associated) THEN

  Line 1379:                   p_nh%diag%ddt_vn_cor(je,jk,jb)=  p_nh%diag%ddt_vn_cor(je,jk,jb) + r_nsubsteps * z_ddt_vn_cor

  Line 1384:               IF (p_nh%diag%ddt_vn_pgr_is_associated) THEN

  Line 1385:                 p_nh%diag%ddt_vn_pgr(je,jk,jb)  =  p_nh%diag%ddt_vn_pgr(je,jk,jb) + r_nsubsteps * z_ddt_vn_pgr

  Line 1388:               IF (p_nh%diag%ddt_vn_phd_is_associated) THEN

  Line 1389:                 p_nh%diag%ddt_vn_phd(je,jk,jb)  =  p_nh%diag%ddt_vn_phd(je,jk,jb) + r_nsubsteps * p_nh%diag%ddt_vn_phy(je,jk,jb)

  Line 1392:               IF (p_nh%diag%ddt_vn_dyn_is_associated) THEN

  Line 1393:                 p_nh%diag%ddt_vn_dyn(je,jk,jb)  =  p_nh%diag%ddt_vn_dyn(je,jk,jb) + r_nsubsteps * z_ddt_vn_dyn

  Line 1405:             DO je = i_startidx, i_endidx

  Line 1408:                 &                                ( p_nh%diag%ddt_vn_apc_pc(je,jk,jb,nnow)                   &

  Line 1410:                 &                                 +p_nh%diag%ddt_vn_phy(je,jk,jb)                        )

  Line 1423:             DO je = i_startidx, i_endidx

  Line 1434:               DO je = i_startidx, i_endidx

  Line 1452:               DO je = i_startidx, i_endidx

  Line 1463:                 IF (p_nh%diag%ddt_vn_dmp_is_associated) THEN

  Line 1464:                   p_nh%diag%ddt_vn_dmp(je,jk,jb)  =  p_nh%diag%ddt_vn_dmp(je,jk,jb) + z_d_vn_dmp * r_dtimensubsteps

  Line 1467:                 IF (p_nh%diag%ddt_vn_dyn_is_associated) THEN

  Line 1468:                   p_nh%diag%ddt_vn_dyn(je,jk,jb)  =  p_nh%diag%ddt_vn_dyn(je,jk,jb) + z_d_vn_dmp * r_dtimensubsteps

  Line 1486:                 DO je = i_startidx, i_endidx

  Line 1493:                   IF (p_nh%diag%ddt_vn_dmp_is_associated) THEN

  Line 1494:                     p_nh%diag%ddt_vn_dmp(je,jk,jb)  =  p_nh%diag%ddt_vn_dmp(je,jk,jb) + z_d_vn_dmp * r_dtimensubsteps

  Line 1497:                   IF (p_nh%diag%ddt_vn_dyn_is_associated) THEN

  Line 1498:                     p_nh%diag%ddt_vn_dyn(je,jk,jb)  =  p_nh%diag%ddt_vn_dyn(je,jk,jb) + z_d_vn_dmp * r_dtimensubsteps

  Line 1509:                 DO je = i_startidx, i_endidx

  Line 1516:                   IF (p_nh%diag%ddt_vn_dmp_is_associated) THEN

  Line 1517:                     p_nh%diag%ddt_vn_dmp(je,jk,jb)  =  p_nh%diag%ddt_vn_dmp(je,jk,jb) + z_d_vn_dmp * r_dtimensubsteps

  Line 1520:                   IF (p_nh%diag%ddt_vn_dyn_is_associated) THEN

  Line 1521:                     p_nh%diag%ddt_vn_dyn(je,jk,jb)  =  p_nh%diag%ddt_vn_dyn(je,jk,jb) + z_d_vn_dmp * r_dtimensubsteps

  Line 1536:             DO je = i_startidx, i_endidx

  Line 1544:                 IF (p_nh%diag%ddt_vn_iau_is_associated) THEN

  Line 1545:                   p_nh%diag%ddt_vn_iau(je,jk,jb)  =  p_nh%diag%ddt_vn_iau(je,jk,jb) + z_d_vn_iau * r_dtimensubsteps

  Line 1548:                 IF (p_nh%diag%ddt_vn_dyn_is_associated) THEN

  Line 1549:                   p_nh%diag%ddt_vn_dyn(je,jk,jb)  =  p_nh%diag%ddt_vn_dyn(je,jk,jb) + z_d_vn_iau * r_dtimensubsteps

  Line 1564:           !$ACC LOOP GANG VECTOR COLLAPSE(2) PRIVATE(z_ddt_vn_ray)

  Line 1567:             DO je = i_startidx, i_endidx


(Results truncated. Consider using a more specific path or pattern.)
```

**Tool: grep**

**Input:**
```json
{
  "include": "mo_general_nh_interface.f90",
  "pattern": "ddt|tend|process"
}
```

**Output:**
```
No files found
```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 19.5s)

_Thinking:_

I have enough from the key code. The core update in `mo_solve_nonhydro.f90` around line 1367:

```
p_nh%prog(nnew)%vn(je,jk,jb) = p_nh%prog(nnow)%vn(je,jk,jb) + dtime * z_ddt_vn_dyn
```

where
```
z_ddt_vn_dyn = z_ddt_vn_apc (advection + Coriolis) + z_ddt_vn_pgr (pressure gradient) + p_nh%diag%ddt_vn_phy (physics applied in dynamics)
```

So the characterization `u^{n+1} = u^n + Δt[(du/dt)_dynamics + (du/dt)_GWD + (du/dt)_turbulence + ...]` is roughly accurate in that physics tendencies enter as additive tendencies in the same time step. But there are important caveats:

1. The time stepping is not a simple forward Euler — it's a predictor-corrector (two-stage) scheme with semi-implicit treatment of fast waves. Line 421: "istep == 1 predictor step".

2. The GWD and turbulence tendencies are NOT directly evaluated for every u at the "dynamics" dt; they are computed in separate physics calls with their own time intervals (dt_gwd, dt_turb), and then combined. Actually, turbdiff both computes tendencies AND applies them directly (fast physics applied within the physics interface). Let me check.

Actually, from the turbdiff interface, turbulence tendencies are applied within the fast physics (p_diag%u += tcall_turb * ddt_u_turb). This happens in the fast physics rather than being passed to dynamics as ddt.

In the NWP model, there's a split: "fast physics" (turbulence, SSO/GWD often done in a way where they're directly applied/relaxation) vs "slow physics" (radiation, convection) which are added as tendencies to the dynamics driver.

Let me check what ddt_vn_phy contains and how it's assembled in mo_nh_interface_nwp.f90.

Let me look at that. Also, there's the point that the dynamics time step involves substepping (r_nsubsteps), time weights (wgt_nnow_vel, wgt_nnew_vel), so ddt_vn_phy is evaluated at the appropriate time level.

The key caveats for the characterization:

1. ICON dynamical core uses a **two-stage predictor-corrector with semi-implicit time integration** (not simple explicit Euler). Physics tendencies enter in both predictor and corrector steps (with time weighting).

2. **Not all physics is added as "tendencies in the momentum equation"**. In the ICON NWP setup, turbulence (turbdiff/turbtrans) and microphysics are "fast physics" applied directly within the physics interface (they update state directly: p_diag%u += dt*ddt_u_turb). Slow physics (radiation, convection, GWD?) produce tendencies that are passed to dynamics as ddt_vn_phy, added to the dynamics driver `solve_nh`.

Actually let me check where ddt_vn_phy is set up in the NWP interface, and whether GWD turbulence go there or are applied directly.

Let me look at mo_nh_interface_nwp.f90 around where ddt_vn_phy is filled.

Let me check how `ddt_vn_phy` is assembled in the NWP interface



**Tool: grep**

**Input:**
```json
{
  "include": "mo_nh_interface_nwp.f90",
  "pattern": "ddt_vn_phy|ddt_vn_phm|ddt_vn_imp|ddt_vn_adv|ddt_vn_pgr|ddt_vn_cor"
}
```

**Output:**
```
Found 3 matches
/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90:
  Line 2804:     !!      ddt_vn_phy  =interpol(ddt_u_tot)+interpol(ddt_v_tot)

  Line 2844:             pt_diag%ddt_vn_phy(jce,jk,jb) =   pt_int_state%c_lin_e(jce,1,jb)           &

  Line 2914:             pt_diag%ddt_vn_phy(jce,jk,jb) =   pt_int_state%c_lin_e(jce,1,jb) &

```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 66.8s)

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90",
  "offset": 2780,
  "limit": 160
}
```

**Output:**
```
<path>/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90</path>
<type>file</type>
<content>
2780: 
2781:               IF (p_metrics%mask_prog_halo_c(jc,jb)) THEN
2782:                 pt_prog%exner(jc,jk,jb) = EXP(rd_o_cpd*LOG(rd_o_p0ref                   &
2783:                   &                     * pt_prog%rho(jc,jk,jb)*pt_diag%tempv(jc,jk,jb)))
2784: 
2785:                 pt_prog%theta_v(jc,jk,jb) = pt_diag%tempv(jc,jk,jb) &
2786:                   &                       / pt_prog%exner(jc,jk,jb)
2787: 
2788:               ENDIF
2789: 
2790:             ENDDO
2791:           ENDDO
2792:           !$ACC END PARALLEL
2793:         ENDDO
2794: !$OMP END DO NOWAIT
2795: !$OMP END PARALLEL
2796: 
2797:       ENDIF ! my_process_is_mpi_all_parallel
2798:     ENDIF ! fast-physics synchronization
2799:     IF (timers_level > 10) CALL timer_stop(timer_phys_acc_par)
2800: 
2801:     !-------------------------------------------------------------------------
2802:     !>
2803:     !!    Interpolation from  u,v onto v_n
2804:     !!      ddt_vn_phy  =interpol(ddt_u_tot)+interpol(ddt_v_tot)
2805:     !!      Calculate normal velocity at edge midpoints
2806:     !-------------------------------------------------------------------------
2807: 
2808:     IF (timers_level > 10)  CALL timer_start(timer_phys_acc_2)
2809: !$OMP PARALLEL PRIVATE(rl_start,rl_end,i_startblk,i_endblk)
2810: 
2811:     ! exclude boundary interpolation zone of nested domains
2812:     rl_start = grf_bdywidth_e+1
2813:     rl_end   = min_rledge_int
2814: 
2815:     i_startblk = pt_patch%edges%start_block(rl_start)
2816:     i_endblk   = pt_patch%edges%end_block(rl_end)
2817: 
2818: 
2819: !$OMP DO PRIVATE(jb,jk,jce,i_startidx,i_endidx) ICON_OMP_DEFAULT_SCHEDULE
2820: 
2821:     DO jb = i_startblk, i_endblk
2822: 
2823:       CALL get_indices_e(pt_patch, jb, i_startblk, i_endblk, &
2824:                          i_startidx, i_endidx, rl_start, rl_end)
2825: 
2826: #ifndef __NO_ICON_LES__
2827:       IF ( (is_ls_forcing .OR. l_any_slowphys) .AND. lcall_phy_jg(itturb) ) THEN
2828: #else
2829:       IF ( l_any_slowphys .AND. lcall_phy_jg(itturb) ) THEN
2830: #endif
2831: 
2832:         !$ACC PARALLEL DEFAULT(PRESENT) ASYNC(1) IF(lacc)
2833:         !$ACC LOOP GANG VECTOR COLLAPSE(2)
2834: #ifdef __LOOP_EXCHANGE
2835:         DO jce = i_startidx, i_endidx
2836: !DIR$ IVDEP
2837:           DO jk = 1, nlev
2838: #else
2839: !CDIR UNROLL=5
2840:         DO jk = 1, nlev
2841:           DO jce = i_startidx, i_endidx
2842: #endif
2843: 
2844:             pt_diag%ddt_vn_phy(jce,jk,jb) =   pt_int_state%c_lin_e(jce,1,jb)           &
2845: &                                 * ( z_ddt_u_tot(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &
2846: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,1)%v1  &
2847: &                                   + z_ddt_v_tot(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &
2848: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,1)%v2 )&
2849: &                                                 + pt_int_state%c_lin_e(jce,2,jb)     &
2850: &                                 * ( z_ddt_u_tot(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2851: &                                    * pt_patch%edges%primal_normal_cell(jce,jb,2)%v1  &
2852: &                                  +  z_ddt_v_tot(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2853: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,2)%v2 )
2854: 
2855:             pt_prog%vn(jce,jk,jb) = pt_prog%vn(jce,jk,jb) + dt_loc * (                 &
2856:                                               pt_int_state%c_lin_e(jce,1,jb)           &
2857: &                     * ( prm_nwp_tend%ddt_u_turb(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &
2858: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,1)%v1  &
2859: &                       + prm_nwp_tend%ddt_v_turb(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &
2860: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,1)%v2 )&
2861: &                                                 + pt_int_state%c_lin_e(jce,2,jb)     &
2862: &                     * ( prm_nwp_tend%ddt_u_turb(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2863: &                                    * pt_patch%edges%primal_normal_cell(jce,jb,2)%v1  &
2864: &                      +  prm_nwp_tend%ddt_v_turb(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2865: &                                  *  pt_patch%edges%primal_normal_cell(jce,jb,2)%v2 ) )
2866: 
2867:           ENDDO
2868:         ENDDO
2869:         !$ACC END PARALLEL
2870: 
2871:       ELSE IF (lcall_phy_jg(itturb) ) THEN
2872:         !$ACC PARALLEL DEFAULT(PRESENT) ASYNC(1) IF(lacc)
2873:         !$ACC LOOP GANG VECTOR COLLAPSE(2)
2874: #ifdef __LOOP_EXCHANGE
2875:         DO jce = i_startidx, i_endidx
2876: !DIR$ IVDEP
2877:           DO jk = 1, nlev
2878: #else
2879: !CDIR UNROLL=8
2880:         DO jk = 1, nlev
2881:           DO jce = i_startidx, i_endidx
2882: #endif
2883: 
2884:             pt_prog%vn(jce,jk,jb) = pt_prog%vn(jce,jk,jb) + dt_loc * (                 &
2885:                                               pt_int_state%c_lin_e(jce,1,jb)           &
2886: &                     * ( prm_nwp_tend%ddt_u_turb(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &
2887: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,1)%v1  &
2888: &                       + prm_nwp_tend%ddt_v_turb(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &
2889: &                                   *  pt_patch%edges%primal_normal_cell(jce,jb,1)%v2 )&
2890: &                                                 + pt_int_state%c_lin_e(jce,2,jb)     &
2891: &                     * ( prm_nwp_tend%ddt_u_turb(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2892: &                                    * pt_patch%edges%primal_normal_cell(jce,jb,2)%v1  &
2893: &                      +  prm_nwp_tend%ddt_v_turb(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2894: &                                  *  pt_patch%edges%primal_normal_cell(jce,jb,2)%v2 ) )
2895: 
2896:           ENDDO
2897:         ENDDO
2898:         !$ACC END PARALLEL
2899: #ifdef __MSGWAM
2900: ! MS-GWaM: Handles remaining cases with standalone LS forcing or slow physics when turbulence is off
2901: 
2902:       ELSE IF (lmsgwam(jg) .AND. (is_ls_forcing .OR. l_any_slowphys)) THEN
2903: 
2904: #ifdef __LOOP_EXCHANGE
2905:         DO jce = i_startidx, i_endidx
2906: !DIR$ IVDEP
2907:           DO jk = 1, nlev
2908: #else
2909: !CDIR UNROLL=5
2910:         DO jk = 1, nlev
2911:           DO jce = i_startidx, i_endidx
2912: #endif
2913: 
2914:             pt_diag%ddt_vn_phy(jce,jk,jb) =   pt_int_state%c_lin_e(jce,1,jb) &
2915: &                                 * (z_ddt_u_tot(iidx(jce,jb,1),jk,iblk(jce,jb,1))   &
2916: &                                   * pt_patch%edges%primal_normal_cell(jce,jb,1)%v1  &
2917: &                                   + z_ddt_v_tot(iidx(jce,jb,1),jk,iblk(jce,jb,1))   &
2918: &                                   * pt_patch%edges%primal_normal_cell(jce,jb,1)%v2 )&
2919: &                                                 + pt_int_state%c_lin_e(jce,2,jb)    &
2920: &                                 * (z_ddt_u_tot(iidx(jce,jb,2),jk,iblk(jce,jb,2))   &
2921: &                                    * pt_patch%edges%primal_normal_cell(jce,jb,2)%v1 &
2922: &                                  + z_ddt_v_tot(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &
2923: &                                   * pt_patch%edges%primal_normal_cell(jce,jb,2)%v2 )
2924: 
2925:           ENDDO
2926:         ENDDO
2927: 
2928:       ENDIF
2929: 
2930:       ! MS-GWaM diag printouts to see what's going on
2931:       IF (msg_level >= 15) THEN
2932:         CALL debug_on()
2933:         DO jk = 1,nlev
2934:           WRITE(message_text,'(a,2i4,2E12.4)') 'interface, jk, jb, u, v:',  &
2935:                             jk, jb, pt_diag%u(1,jk,jb), pt_diag%v(1,jk,jb)
2936:           CALL message('', TRIM(message_text))
2937:         ENDDO
2938:         CALL debug_off()
2939: #endif

(Showing lines 2780-2939 of 3059. Use offset=2940 to continue.)
</content>
```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 35.4s)

_Thinking:_

Let me look at how z_ddt_u_tot and z_ddt_v_tot are constructed, and where ddt_vn_phy gets used in solve_nh. Let me search for the construction of z_ddt_u_tot.

Let me see how the total physics tendency `z_ddt_u_tot` is assembled

**Tool: grep**

**Input:**
```json
{
  "include": "mo_nh_interface_nwp.f90",
  "pattern": "z_ddt_u_tot|ddt_u_conv|ddt_u_gwd|ddt_u_sso|ddt_u_vdf|ddt_u_ss|ddt_temp_con|ddt_u_*:"
}
```

**Output:**
```
Found 17 matches
/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90:
  Line 278:       & z_ddt_u_tot (nproma,pt_patch%nlev,pt_patch%nblks_c),&

  Line 407:     !$ACC   CREATE(zcosmu0, z_ddt_u_tot, z_exner_sv, z_qsum, kc_entr_zone) IF(lacc)

  Line 2380:         CALL init(z_ddt_u_tot, lacc=lacc)

  Line 2454:               z_ddt_u_tot(jc,jk,jb) = prm_nwp_tend%ddt_u_gwd(jc,jk,jb) + zddt_u_raylfric(jc,jk)  &

  Line 2455:                 &     + prm_nwp_tend%ddt_u_sso(jc,jk,jb)  + prm_nwp_tend%ddt_u_pconv(jc,jk,jb)

  Line 2463:           z_ddt_u_tot(i_startidx:i_endidx,:,jb) = 0._wp

  Line 2496:                                                         (prm_nwp_tend%ddt_u_sso(jc,jk,jb)+        &

  Line 2497:                                                         prm_nwp_tend%ddt_u_gwd(jc,jk,jb)+         &

  Line 2523:                                                           (prm_nwp_tend%ddt_u_sso(jc,jk,jb)+      &

  Line 2578:                          z_ddt_temp(:,:), z_ddt_u_tot(:,:,jb), z_ddt_v_tot(:,:,jb), lacc=lacc)

  Line 2652:                & z_ddt_u_tot      = z_ddt_u_tot(:,:,jb)                   , & !INOUT

  Line 2733:       CALL sync_patch_array_mult(SYNC_C1, pt_patch, 4, lacc=lacc, f3din1=z_ddt_u_tot, f3din2=z_ddt_v_tot, &

  Line 2745:       CALL sync_patch_array_mult(SYNC_C1, pt_patch, 2, lacc=lacc, f3din1=z_ddt_u_tot, f3din2=z_ddt_v_tot)

  Line 2845: &                                 * ( z_ddt_u_tot(iidx(jce,jb,1),jk,iblk(jce,jb,1))    &

  Line 2850: &                                 * ( z_ddt_u_tot(iidx(jce,jb,2),jk,iblk(jce,jb,2))    &

  Line 2915: &                                 * (z_ddt_u_tot(iidx(jce,jb,1),jk,iblk(jce,jb,1))   &

  Line 2920: &                                 * (z_ddt_u_tot(iidx(jce,jb,2),jk,iblk(jce,jb,2))   &

```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 27.5s)

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90",
  "limit": 100,
  "offset": 2440
}
```

**Output:**
```
<path>/home/jf01/dev/iap/icon-model-public/src/atm_phy_nwp/mo_nh_interface_nwp.f90</path>
<type>file</type>
<content>
2440:           !$ACC END KERNELS
2441:         ENDIF
2442: 
2443: 
2444:         ! Accumulate wind tendencies of slow physics
2445:         ! Strictly spoken, this would not be necessary if only radiation was called
2446:         ! in the current time step, but the radiation time step should be a multiple
2447:         ! of the convection time step anyway in order to obtain up-to-date cloud cover fields
2448:         IF (l_any_slowphys) THEN
2449:           !$ACC PARALLEL DEFAULT(PRESENT) ASYNC(1) IF(lacc)
2450:           !$ACC LOOP GANG VECTOR COLLAPSE(2)
2451:           DO jk = 1, nlev
2452: !DIR$ IVDEP
2453:             DO jc = i_startidx, i_endidx
2454:               z_ddt_u_tot(jc,jk,jb) = prm_nwp_tend%ddt_u_gwd(jc,jk,jb) + zddt_u_raylfric(jc,jk)  &
2455:                 &     + prm_nwp_tend%ddt_u_sso(jc,jk,jb)  + prm_nwp_tend%ddt_u_pconv(jc,jk,jb)
2456:               z_ddt_v_tot(jc,jk,jb) = prm_nwp_tend%ddt_v_gwd(jc,jk,jb) + zddt_v_raylfric(jc,jk)  &
2457:                 &     + prm_nwp_tend%ddt_v_sso(jc,jk,jb)  + prm_nwp_tend%ddt_v_pconv(jc,jk,jb)
2458:             ENDDO
2459:           ENDDO
2460:           !$ACC END PARALLEL
2461: #ifndef __NO_ICON_LES__
2462:         ELSE IF (is_ls_forcing) THEN
2463:           z_ddt_u_tot(i_startidx:i_endidx,:,jb) = 0._wp
2464:           z_ddt_v_tot(i_startidx:i_endidx,:,jb) = 0._wp
2465: #endif
2466:         ENDIF
2467: 
2468:         IF (itype_dissip_heat >= 1) THEN ! add dissipative heating for SSO, GWD and Rayleigh friction
2469: 
2470:           ! SQRT of Richardson number between the two lowest model levels
2471:           ! This is used below to reduce frictional heating near the surface under very stable conditions
2472:           !$ACC PARALLEL DEFAULT(PRESENT) ASYNC(1) IF(lacc)
2473:           !$ACC LOOP GANG VECTOR PRIVATE(n2, dvdz2)
2474:           DO jc = i_startidx, i_endidx
2475:             n2 = 2._wp*grav/(pt_prog%theta_v(jc,nlev,jb)+pt_prog%theta_v(jc,nlev-1,jb)) * MAX(1.e-4_wp,        &
2476:                  (pt_prog%theta_v(jc,nlev-1,jb)-pt_prog%theta_v(jc,nlev,jb))/p_metrics%ddqz_z_half(jc,nlev,jb) )
2477:             dvdz2 = MAX(1.e-6_wp, ( (pt_diag%u(jc,nlev-1,jb)-pt_diag%u(jc,nlev,jb))**2 +                      &
2478:                     (pt_diag%v(jc,nlev-1,jb)-pt_diag%v(jc,nlev,jb))**2 )/p_metrics%ddqz_z_half(jc,nlev,jb)**2 )
2479:             sqrt_ri(jc) = MAX(1._wp, SQRT(n2/dvdz2))
2480:           ENDDO
2481:           !$ACC END PARALLEL
2482: 
2483: #ifndef __MSGWAM
2484:           IF (.NOT. lmsgwam(jg)) THEN
2485: #else
2486:           ! Case for MSGWAM switched off, or used offline, or used without frictional heat
2487:           IF (.NOT. lmsgwam(jg) .OR. lmsgwam_offline .OR. lmsgwam_fricheat) THEN
2488: #endif
2489:             !$ACC PARALLEL DEFAULT(PRESENT) ASYNC(1) IF(lacc)
2490:             !$ACC LOOP GANG VECTOR PRIVATE(wfac) COLLAPSE(2)
2491:             DO jk = 1, nlev
2492: !DIR$ IVDEP
2493:               DO jc = i_startidx, i_endidx
2494:                 wfac = MIN(1._wp, 0.004_wp*p_metrics%geopot_agl(jc,jk,jb)/grav)
2495:                 z_ddt_temp_drag(jc,jk)               = -rcvd*(pt_diag%u(jc,jk,jb)*                &
2496:                                                         (prm_nwp_tend%ddt_u_sso(jc,jk,jb)+        &
2497:                                                         prm_nwp_tend%ddt_u_gwd(jc,jk,jb)+         &
2498:                                                         zddt_u_raylfric(jc,jk))                   &
2499:                                                         +      pt_diag%v(jc,jk,jb)*               &
2500:                                                         (prm_nwp_tend%ddt_v_sso(jc,jk,jb)+        &
2501:                                                         prm_nwp_tend%ddt_v_gwd(jc,jk,jb)+         &
2502:                                                         zddt_v_raylfric(jc,jk)) ) /               &
2503:                                                         ((1._wp-wfac)*sqrt_ri(jc) + wfac)
2504:                 !
2505:                 ! total slow physics heating rate
2506:                 !
2507:                 z_ddt_temp(jc,jk) = prm_nwp_tend%ddt_temp_radsw(jc,jk,jb) + prm_nwp_tend%ddt_temp_radlw(jc,jk,jb) &
2508:                   &               + z_ddt_temp_drag(jc,jk)                + prm_nwp_tend%ddt_temp_pconv(jc,jk,jb) &
2509:                   &               + prm_nwp_tend%ddt_temp_clcov(jc,jk,jb)
2510:               ENDDO
2511:             ENDDO
2512:           !$ACC END PARALLEL
2513:           ELSE
2514:             DO jk = 1, nlev
2515:               DO jc = i_startidx, i_endidx
2516:                 l_out_ddt_temp_drag = .TRUE.
2517:                 ! i.e. on-line MS-GWaM without frictional heating
2518:                 ! heating related to momentum deposition by SSO, GWD and Rayleigh friction
2519:                     z_ddt_temp_drag(jc,jk) = prm_nwp_tend%ddt_temp_drag(jc,jk,jb)
2520:                     wfac = MIN(1._wp, 0.004_wp*p_metrics%geopot_agl(jc,jk,jb)/grav)
2521:                     z_ddt_temp_drag(jc,jk)               =  z_ddt_temp_drag(jc,jk)                &
2522:                                                           -rcvd*(pt_diag%u(jc,jk,jb)*             &
2523:                                                           (prm_nwp_tend%ddt_u_sso(jc,jk,jb)+      &
2524:                                                             zddt_u_raylfric(jc,jk))               &
2525:                                                           +      pt_diag%v(jc,jk,jb)*             &
2526:                                                           (prm_nwp_tend%ddt_v_sso(jc,jk,jb)+      &
2527:                                                             zddt_v_raylfric(jc,jk)) ) /           &
2528:                                                             ((1._wp-wfac)*sqrt_ri(jc) + wfac)
2529:                               !
2530:                 ! total slow physics heating rate
2531:                 !
2532:                 z_ddt_temp(jc,jk) = prm_nwp_tend%ddt_temp_radsw(jc,jk,jb) + prm_nwp_tend%ddt_temp_radlw(jc,jk,jb) &
2533:                   &               + z_ddt_temp_drag(jc,jk)                + prm_nwp_tend%ddt_temp_pconv(jc,jk,jb) &
2534:                   &               + prm_nwp_tend%ddt_temp_clcov(jc,jk,jb)
2535:               ENDDO
2536:             ENDDO
2537:           ENDIF
2538: 
2539:         ELSE

(Showing lines 2440-2539 of 3059. Use offset=2540 to continue.)
</content>
```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 23.9s)

_Thinking:_

This is very informative. So in the NWP physics, the wind tendencies that go into `ddt_vn_phy` (slow physics) are: GWD + Rayleigh friction + SSO + convection (pconv). Turbulence (ddt_u_turb) is applied directly to vn state in the interface (`pt_prog%vn = pt_prog%vn + dt_loc * turb tendency`) — it's a "fast physics" treatment. 

Now let me check the dynamics time stepping scheme itself. Let me look at the predictor/corrector and time weights in mo_solve_nonhydro.f90. Line 421 says "istep == 1 predictor step". Let me look at the overall time integration structure.

Actually, I already have a very good picture. Let me also check the time-level weights (wgt_nnow_vel, wgt_nnew_vel) which relate to the semi-implicit time weighting, and how the scheme is (predictor-corrector, semi-implicit). Let me look at the main loop over substeps in solve_nh, around lines 400-450.

Let me check the time-stepping structure (predictor-corrector, weights) in the dynamics

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/jf01/dev/iap/icon-model-public/src/atm_dyn_iconam/mo_solve_nonhydro.f90",
  "limit": 100,
  "offset": 380
}
```

**Output:**
```
<path>/home/jf01/dev/iap/icon-model-public/src/atm_dyn_iconam/mo_solve_nonhydro.f90</path>
<type>file</type>
<content>
380:     !$ACC   CREATE(z_dexner_dz_c, z_exner_ex_pr, z_gradh_exner, z_rth_pr, z_grad_rth) &
381:     !$ACC   CREATE(z_theta_v_pr_ic, z_th_ddz_exner_c, z_w_concorr_mc) &
382:     !$ACC   CREATE(z_vn_avg, z_rho_e, z_theta_v_e, z_dwdz_dd, z_mflx_top) &
383:     !$ACC   CREATE(z_exner_ic, z_alpha, z_beta, z_q, z_contr_w_fl_l, z_exner_expl) &
384:     !$ACC   CREATE(z_flxdiv_mass, z_flxdiv_theta, z_rho_expl, z_w_expl) &
385:     !$ACC   CREATE(z_rho_v, z_theta_v_v, z_graddiv_vn, z_hydro_corr, z_graddiv2_vn) &
386:     !$ACC   COPYIN(nflatlev, nflat_gradp, kstart_dd3d, kstart_moist, nrdmax) &
387:     !$ACC   COPYIN(z_raylfac, ndyn_substeps_var, scal_divdamp, bdy_divdamp) &
388:     !$ACC   PRESENT(prep_adv, p_int, p_patch, p_nh) &
389:     !$ACC   PRESENT(icidx, icblk, ividx, ivblk, ieidx, ieblk, ikidx, iqidx, iqblk) &
390:     !$ACC   PRESENT(ipeidx, ipeblk, iplev)
391: 
392:     ! scaling factor for second-order divergence damping: divdamp_fac_o2*delta_x**2
393:     ! delta_x**2 is approximated by the mean cell area
394:     scal_divdamp_o2 = divdamp_fac_o2 * p_patch%geometry_info%mean_cell_area
395: 
396: 
397:     IF (p_test_run) THEN
398:       !$ACC KERNELS DEFAULT(PRESENT) ASYNC(1)
399:       z_rho_e     = 0._wp
400:       z_theta_v_e = 0._wp
401:       z_dwdz_dd   = 0._wp
402:       z_graddiv_vn= 0._wp
403:       !$ACC END KERNELS
404:     ENDIF
405: 
406:     ! Weighting coefficients for velocity advection if tendency averaging is used
407:     ! The off-centering specified here turned out to be beneficial to numerical
408:     ! stability in extreme situations
409:     wgt_nnow_vel = 0.5_wp - veladv_offctr ! default value for veladv_offctr is 0.25
410:     wgt_nnew_vel = 0.5_wp + veladv_offctr
411: 
412:     ! Weighting coefficients for rho and theta at interface levels in the corrector step
413:     ! This empirically determined weighting minimizes the vertical wind off-centering
414:     ! needed for numerical stability of vertical sound wave propagation
415:     wgt_nnew_rth = 0.5_wp + rhotheta_offctr ! default value for rhotheta_offctr is -0.1
416:     wgt_nnow_rth = 1._wp - wgt_nnew_rth
417: 
418: !$NEC sparse
419:     DO istep = 1, 2
420: 
421:       IF (istep == 1) THEN ! predictor step
422:         IF (itime_scheme >= 6 .OR. l_init .OR. l_recompute) THEN
423:           IF (itime_scheme < 6 .AND. .NOT. l_init) THEN
424:             lvn_only = .TRUE. ! Recompute only vn tendency
425:           ELSE
426:             lvn_only = .FALSE.
427:           ENDIF
428:           CALL velocity_tendencies(p_nh%prog(nnow),p_patch,p_int,p_nh%metrics,p_nh%diag,z_w_concorr_me, &
429:             z_kin_hor_e,z_vt_ie,nnow,istep,lvn_only,dtime,dt_linintp_ubc_nnow,ldeepatmo)
430:         ENDIF
431:         nvar = nnow
432:       ELSE                 ! corrector step
433:         lvn_only = .FALSE.
434:         CALL velocity_tendencies(p_nh%prog(nnew),p_patch,p_int,p_nh%metrics,p_nh%diag,z_w_concorr_me, &
435:           z_kin_hor_e,z_vt_ie,nnew,istep,lvn_only,dtime,dt_linintp_ubc_nnew,ldeepatmo)
436:         nvar = nnew
437:       ENDIF
438: 
439:       ! Preparations for igradp_method = 3/5 (reformulated extrapolation below the ground)
440:       IF (istep == 1 .AND. (igradp_method == 3 .OR. igradp_method == 5)) THEN
441: 
442:         nproma_gradp = cpu_min_nproma(nproma,256)
443:         nblks_gradp  = INT(p_nh%metrics%pg_listdim/nproma_gradp)
444:         npromz_gradp = MOD(p_nh%metrics%pg_listdim,nproma_gradp)
445:         IF (npromz_gradp > 0) THEN
446:           nblks_gradp = nblks_gradp + 1
447:         ELSE
448:           npromz_gradp = nproma_gradp
449:         ENDIF
450: 
451:       ENDIF
452: 
453:       IF (timers_level > 5) CALL timer_start(timer_solve_nh_cellcomp)
454: 
455:       ! Computations on mass points
456: !$OMP PARALLEL PRIVATE (rl_start,rl_end,i_startblk,i_endblk)
457: 
458:       rl_start = 3
459:       IF (istep == 1) THEN
460:         rl_end = min_rlcell_int - 1
461:       ELSE ! halo points are not needed in step 2
462:         rl_end = min_rlcell_int
463:       ENDIF
464: 
465:       i_startblk = p_patch%cells%start_block(rl_start)
466:       i_endblk   = p_patch%cells%end_block(rl_end)
467: 
468:       ! initialize nest boundary points of z_rth_pr with zero
469:       IF (istep == 1 .AND. (jg > 1 .OR. l_limited_area)) THEN
470: 
471: #ifdef __SWAPDIM
472:           ! Abusing contiguous memory here by going out of bounds, init_zero_contiguous expects 1d array
473: #if defined(__SINGLE_PRECISION) || defined(__MIXED_PRECISION)
474:           CALL init_zero_contiguous_sp(z_rth_pr(1,1,1,1), nproma*nlev*i_startblk, opt_acc_async=.TRUE., &
475:             & lacc=.TRUE.)
476:           CALL init_zero_contiguous_sp(z_rth_pr(1,1,1,2), nproma*nlev*i_startblk, opt_acc_async=.TRUE., &
477:             & lacc=.TRUE.)
478: #else
479:           CALL init_zero_contiguous_dp(z_rth_pr(1,1,1,1), nproma*nlev*i_startblk, opt_acc_async=.TRUE., &

(Showing lines 380-479 of 3166. Use offset=480 to continue.)
</content>
```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 19.0s)

_Thinking:_

Now let me look at the final velocity update to see the "physics applied in dynamics" part - already saw line 1365-1367. Let me look more closely at the update around there for how physics tendency is added and how the time weighting works.

Let me read the core velocity update section

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/jf01/dev/iap/icon-model-public/src/atm_dyn_iconam/mo_solve_nonhydro.f90",
  "limit": 90,
  "offset": 1340
}
```

**Output:**
```
<path>/home/jf01/dev/iap/icon-model-public/src/atm_dyn_iconam/mo_solve_nonhydro.f90</path>
<type>file</type>
<content>
1340:       ! Update horizontal velocity field: advection, Coriolis force, pressure-gradient term, and physics
1341: 
1342: !$OMP DO PRIVATE(jb,i_startidx,i_endidx,jk,je,z_graddiv2_vn,                                                   &
1343: !$OMP            z_ddt_vn_dyn, z_ddt_vn_apc, z_ddt_vn_cor, z_ddt_vn_pgr, z_ddt_vn_ray, z_d_vn_dmp, z_d_vn_iau  &
1344: !$OMP           ) ICON_OMP_DEFAULT_SCHEDULE
1345: 
1346:       DO jb = i_startblk, i_endblk
1347: 
1348:         CALL get_indices_e(p_patch, jb, i_startblk, i_endblk, &
1349:           i_startidx, i_endidx, rl_start, rl_end)
1350: 
1351:         !$ACC PARALLEL DEFAULT(PRESENT) ASYNC(1)
1352:         IF (istep == 2) THEN ! use temporally averaged velocity advection terms
1353: 
1354:           !$ACC LOOP GANG(STATIC: 1) VECTOR PRIVATE(z_ddt_vn_dyn, z_ddt_vn_apc, z_ddt_vn_cor, z_ddt_vn_pgr) TILE(32, 4)
1355:           DO jk = 1, nlev
1356: !DIR$ IVDEP
1357:             DO je = i_startidx, i_endidx
1358:               !
1359:               z_ddt_vn_apc                      =  p_nh%diag%ddt_vn_apc_pc(je,jk,jb,nnow)*wgt_nnow_vel  &
1360:                 &                                 +p_nh%diag%ddt_vn_apc_pc(je,jk,jb,nnew)*wgt_nnew_vel
1361:               z_ddt_vn_pgr                      = -cpd*z_theta_v_e(je,jk,jb)*z_gradh_exner(je,jk,jb)
1362:               !
1363:               z_ddt_vn_dyn                      =  z_ddt_vn_apc                   & ! advection plus Coriolis
1364:                 &                                 +z_ddt_vn_pgr                   & ! pressure gradient
1365:                 &                                 +p_nh%diag%ddt_vn_phy(je,jk,jb)   ! physics applied in dynamics
1366:               !
1367:               p_nh%prog(nnew)%vn(je,jk,jb)      =  p_nh%prog(nnow)%vn(je,jk,jb)   + dtime       * z_ddt_vn_dyn
1368:               !
1369: #ifdef __ENABLE_DDT_VN_XYZ__
1370:               IF (p_nh%diag%ddt_vn_adv_is_associated .OR. p_nh%diag%ddt_vn_cor_is_associated) THEN
1371:                 z_ddt_vn_cor                    =  p_nh%diag%ddt_vn_cor_pc(je,jk,jb,nnow)*wgt_nnow_vel  &
1372:                   &                               +p_nh%diag%ddt_vn_cor_pc(je,jk,jb,nnew)*wgt_nnew_vel
1373:                 !
1374:                 IF (p_nh%diag%ddt_vn_adv_is_associated) THEN
1375:                   p_nh%diag%ddt_vn_adv(je,jk,jb)=  p_nh%diag%ddt_vn_adv(je,jk,jb) + r_nsubsteps *(z_ddt_vn_apc-z_ddt_vn_cor)
1376:                 END IF
1377:                 !
1378:                 IF (p_nh%diag%ddt_vn_cor_is_associated) THEN
1379:                   p_nh%diag%ddt_vn_cor(je,jk,jb)=  p_nh%diag%ddt_vn_cor(je,jk,jb) + r_nsubsteps * z_ddt_vn_cor
1380:                 END IF
1381:                 !
1382:               END IF
1383:               !
1384:               IF (p_nh%diag%ddt_vn_pgr_is_associated) THEN
1385:                 p_nh%diag%ddt_vn_pgr(je,jk,jb)  =  p_nh%diag%ddt_vn_pgr(je,jk,jb) + r_nsubsteps * z_ddt_vn_pgr
1386:               END IF
1387:               !
1388:               IF (p_nh%diag%ddt_vn_phd_is_associated) THEN
1389:                 p_nh%diag%ddt_vn_phd(je,jk,jb)  =  p_nh%diag%ddt_vn_phd(je,jk,jb) + r_nsubsteps * p_nh%diag%ddt_vn_phy(je,jk,jb)
1390:               END IF
1391:               !
1392:               IF (p_nh%diag%ddt_vn_dyn_is_associated) THEN
1393:                 p_nh%diag%ddt_vn_dyn(je,jk,jb)  =  p_nh%diag%ddt_vn_dyn(je,jk,jb) + r_nsubsteps * z_ddt_vn_dyn
1394:               END IF
1395: #endif
1396:               !
1397:             ENDDO
1398:           ENDDO
1399: 
1400:         ELSE
1401: 
1402:           !$ACC LOOP GANG(STATIC: 1) VECTOR TILE(32, 4)
1403:           DO jk = 1, nlev
1404: !DIR$ IVDEP
1405:             DO je = i_startidx, i_endidx
1406:               !
1407:               p_nh%prog(nnew)%vn(je,jk,jb)      =  p_nh%prog(nnow)%vn(je,jk,jb)   + dtime *                 &
1408:                 &                                ( p_nh%diag%ddt_vn_apc_pc(je,jk,jb,nnow)                   &
1409:                 &                                 -cpd*z_theta_v_e(je,jk,jb)*z_gradh_exner(je,jk,jb)        &
1410:                 &                                 +p_nh%diag%ddt_vn_phy(je,jk,jb)                        )
1411:               !
1412:             ENDDO
1413:           ENDDO
1414:         ENDIF
1415: 
1416: 
1417:         IF (istep == 2) THEN
1418: 
1419:           IF (divdamp_order == 4 .OR. divdamp_order == 24) THEN ! fourth-order divergence damping
1420:           ! Compute gradient of divergence of gradient of divergence for fourth-order divergence damping
1421:             !$ACC LOOP GANG(STATIC: 1) VECTOR TILE(32, 4)
1422: #ifdef __LOOP_EXCHANGE
1423:             DO je = i_startidx, i_endidx
1424: !DIR$ IVDEP
1425:               DO jk = 1, nlev
1426:                 z_graddiv2_vn(je,jk) = p_int%geofac_grdiv(je,1,jb)*z_graddiv_vn(jk,je,jb)      &
1427:                   + p_int%geofac_grdiv(je,2,jb)*z_graddiv_vn(jk,iqidx(je,jb,1),iqblk(je,jb,1)) &
1428:                   + p_int%geofac_grdiv(je,3,jb)*z_graddiv_vn(jk,iqidx(je,jb,2),iqblk(je,jb,2)) &
1429:                   + p_int%geofac_grdiv(je,4,jb)*z_graddiv_vn(jk,iqidx(je,jb,3),iqblk(je,jb,3)) &

(Showing lines 1340-1429 of 3166. Use offset=1430 to continue.)
</content>
```

---

## Assistant (Plan · DeepSeek V4 Flash Free · 123.3s)

_Thinking:_

Now I have a good picture. Let me formulate the answer.

Actually let me quickly check what happens in the velocity tendency - is there a separate RK-type scheme or just implicitly handled? Let me check the main time stepping loop and what time_scheme does. Also the "general_nh_interface" that calls solve_nh twice maybe. Let me check quickly the time loop structure - actually I have enough. Let me also check the docs about the split and how fast physics (turbulence) is done vs slow physics.

I already have strong evidence:
- `nwp_nh_interface` line 2454: z_ddt_u_tot = ddt_u_gwd + raylfric + ddt_u_sso + ddt_u_pconv
- Turbulence (`ddt_u_*_turb`) is applied directly (`pt_prog%vn += dt_loc * ...` lines 2855-2894) and also contributes to ddt_vn_phy via z_ddt_u_tot if l_any_slowphys - actually wait, in the first branch (line 2844) ddt_vn_phy is computed with z_ddt_u_tot (which contains gwd+sso+pconv+raylfric), and then ALSO the turb tendency is added separately to pt_prog%vn. So turbulence is applied directly (fast physics) whereas slow physics (rad, conv, gwd, sso) enters as ddt_vn_phy within the dynamics time step.

So the characterization u^{n+1} = u^n + Δt[dynamics + GWD + turbulence + ...] is roughly correct conceptually: physics tendencies are added to the dynamics tendencies and the whole thing scaled by Δt. But there are important caveats:

1. The time stepping in ICON dynamics is a two-step predictor-corrector (istep=1,2), semi-implicit treatment of fast waves (horizontal pressure gradient & divergence are treated semi-implicitly). So it's NOT a single explicit forward Euler step; the dynamics themselves are a predictor-corrector with time-averaged tendencies (wgt_nnow_vel/wgt_nnew_vel).

2. The velocity update equation shown (vn at edges) is exactly: vn(nnew) = vn(nnow) + dtime * (advection+coriolis + pressure gradient + physics(ddt_vn_phy)). So yes for vn.

3. Turbulence is a "fast physics" applied directly to the state (vn += dt_loc * ddt_vn_turb) rather than through ddt_vn_phy in the same way. Actually it's still an additive. But the velocities: turbulence tendency added directly in nwp_nh_interface. GWD/SSO/conv/rad are slow physics entering via ddt_vn_phy inside solve_nh.

So the answer: roughly yes, it's an additive tendency split. Caveats:
- It's not plain forward Euler; the dynamics solve is a two-stage predictor-corrector (semi-implicit) with time weights.
- Physics: some "fast" physics (turbulence, and partially microphysics) are applied directly to the prognostic state (additive), while "slow" physics (radiation, convection, GWD/SSO, Rayleigh friction) are accumulated as ddt_vn_phy and folded into the dynamics tendency, then scaled by the full dtime. All are additive tendencies applied with Δt.
- Also there are substeps (r_nsubsteps), divergence damping, Rayleigh, IAU, etc. added on top.
- For variables other than wind (theta_v, rho, tracers), similar.

So the characterization is essentially right at the conceptual level — it's an additive operator split where physics tendencies are added to the dynamics tendencies each time step. Worth noting the details. Let me be concise but informative.

This is plan mode, but this is really a question, not a task to modify files. I should just answer. Keep it reasonably concise. The user asked "is this roughly accurate?" — the answer is yes, roughly, with caveats. Let me give a focused answer referencing code lines.

I should not create a plan since there's nothing to implement. This is a Q&A. In plan mode I answer the question.

Yes, roughly accurate for the horizontal wind equation — with a few important caveats.

The additive-tendency structure you wrote is exactly what ICON does. In `mo_solve_nonhydro.f90:1363-1367` the velocity update is:

```
vn(nnew) = vn(nnow) + dtime * (advection+Coriolis + pressure-gradient + ddt_vn_phy)
```

where `ddt_vn_phy` is the summed physics tendency passed from `nwp_nh_interface` (mo_nh_interface_nwp.f90:2454-2457):

```
ddt_u_tot = ddt_u_gwd + Rayleigh-friction + ddt_u_sso + ddt_u_pconv
```

Caveats:

1. **It's not forward Euler.** The dynamics is a two-stage predictor–corrector (`DO istep = 1,2` in mo_solve_nonhydro.f90:419), with time-averaged advection terms (`wgt_nnow_vel`/`wgt_nnew_vel`, line 409-410) and semi-implicit treatment of the fast pressure-gradient/divergence modes. The physics tendency `ddt_vn_phy` is folded into both the predictor (line 1407-1410) and corrector (line 1367) steps.

2. **Turbulence enters differently.** `ddt_u_turb` is applied directly to `vn` in the physics interface (`pt_prog%vn += dt_loc * ...`, mo_nh_interface_nwp.f90:2855-2865), not through `ddt_vn_phy`. So your box is still an additive update, but turbulence is "fast physics," applied straight to the state, whereas GWD/SSO/Rayleigh/conv/radiation are "slow physics" accumulated into `ddt_vn_phy`.

3. **Extra terms** on top of the pure RHS: divergence damping, Rayleigh fricition, IAU (initialization) tendencies, and multiple substeps (`r_nsubsteps`) — all just additional additive tendencies scaled by Δt.

So: conceptually correct (physics = additive tendencies added to dynamics tendencies, all scaled by Δt), but the dynamics side is a predictor–corrector semi-implicit scheme, not a single explicit Euler step, and turbulence is split out as fast physics rather than passing through the same `ddt_vn_phy` channel.

---

